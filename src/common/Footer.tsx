import React from "react";

import Theme from "../style/Theme";

function Footer() {
  return (
    <div style={{ minHeight: "300px", background: `${Theme.baseColor}` }}>
      Footer
    </div>
  );
}

export default Footer;
